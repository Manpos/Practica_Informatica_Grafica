#version 330

in vec3 position;
in vec3 color;
in vec2 texCoord;

out vec3 vertexColor;
out vec2 textMap;

uniform mat4 model;
uniform mat4 view;
uniform mat4 proj;

void main(){
	vertexColor = color;
	textMap = texCoord;
	gl_Position = proj * view * model * vec4(position, 1.0);
}